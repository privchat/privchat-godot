# Godot engine cache
.godot/

# godot-cpp is restored by scripts/setup.sh (pinned: branch 4.5 @ 60b5a41,
# https://github.com/godotengine/godot-cpp.git). Kept out of git to avoid
# vendoring ~270M of upstream sources.
native/godot-cpp/

# scons build intermediates
native/*.os
native/src/*.os
native/.sconsign.dblite

# macOS noise
.DS_Store
