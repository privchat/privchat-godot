# plugin.gd — trivial editor plugin entry for the privchat addon.
# All functionality lives in GDExtension classes + autoload-free GDScript
# facades; this plugin exists only so the addon can be enabled/disabled.
@tool
extends EditorPlugin
